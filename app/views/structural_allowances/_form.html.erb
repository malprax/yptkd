<%= simple_form_for(@structural_allowance) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :nama %>
    <%= f.input :satuan %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
